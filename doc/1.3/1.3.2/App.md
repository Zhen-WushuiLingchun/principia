# 版本更新日志

**主版本号：** 1.3
**完整版本号：** 1.3.2
**当前Tag：** v1.3.2
**更新日期：** 2026-04-08

---

## 新增功能
*   新增自动生成Changelog的工作流和脚本，集成DeepSeek API以生成结构化版本更新日志。
*   新增压缩未打包文件夹步骤，并更新上传路径。

## 性能优化
*   改进可执行文件的压缩处理。
*   在构建工作流中增强可执行文件的压缩功能。
*   增强构建工作流，包含可执行文件压缩。

## Bug修复
*   修复工作流中DeepSeek API端点配置。
*   修复 `gen_changelog.py` 脚本的路径。
*   修复用于标签处理和日志记录的changelog工作流。
*   更新tar命令以正确压缩解包后的文件。
*   更新构建工作流，修复版本号设置，使用发布版本号替代发布标签。
*   修复读取ChangeLog步骤，更新输出方式以支持GitHub Actions。

## 代码重构
*   重构 `build.yml` 中的注释以提高清晰度并修复问题。
*   清理 `build.yml`，移除发布相关部分。
*   更新构建工作流，修复版本号设置，优化发布流程，添加ChangeLog读取步骤，删除冗余的发布测试工作流和ChangeLog文件。

## 依赖更新
*   更新 `build.yml` 以排除额外的文件类型。